datasource db {
  provider = "postgresql"
  url      = "postgresql://cyberminds_database_user:@dpg-d1mdq815pdvs73csk04g-a:5432/cyberminds_database"
}

generator client {
  provider = "prisma-client-js"
}

model jobs {
  id          Int       @id @default(autoincrement())
  title       String
  company     String
  location    String?
  job_type    String?
  salary_min  Int?
  salary_max  Int?
  deadline    DateTime?
  description String?
}
